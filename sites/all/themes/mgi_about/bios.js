function changebio(staff){	var rows = [];	jQuery('#hiddenBios .biosViews').each(function() {		rows.push(this.innerHTML)});	var titles = [];	jQuery('#hiddenBios .biosViews .biosViewTitle').each(function() {		titles.push(this.innerHTML);});			for(i = 0; i<rows.length; i++)	{		if(titles[i] == staff)		{					jQuery('#biosViewer').toggle(400);						//jQuery('#biosViewer').delay(100);			jQuery('#biosViewer').html(rows[i]);			jQuery('#biosViewer').toggle(1000);	//		jQuery('#biosViewer').fadeIn(2000).html(rows[i]);		//	jQuery('#biosViewer').show(300);				}	}}	jQuery(document).ready(function() {	var a = jQuery('span.bioTitle a');	for(i = 0; i<a.length; i++)	{		a[i].href = "javascript:changebio('"+a[i].innerHTML+"');";	}	var rows = [];	jQuery('#hiddenBios .biosViews').each(function() {		rows.push(this.innerHTML)});	jQuery('#biosViewer').html(rows[0]);});